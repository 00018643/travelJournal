<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Note - My Travel Journal</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1>My Travel Journal</h1>
    <nav>
      <a href="/">Home</a>
      <a href="/notes/create">Create a New Note</a>
      <% if (user) { %>
        <a href="/profile/<%= user.id %>" class="btn btn-profile">Profile</a>
        <a href="/logout" class="btn btn-logout">Logout</a>
      <% } %>
    </nav>
  </header>

  <main>
    <section class="form-section">
      <h2>Edit Note</h2>
      <p>Hello, <%= user ? user.email : 'Guest' %></p>
      <form action="/notes/<%= note.id %>" method="POST" class="auth-form two-column-form">
        <div class="form-group">
          <label for="title">Title:</label>
          <input type="text" id="title" name="title" value="<%= note.title %>" required>
        </div>
        <div class="form-group">
          <label for="city">City:</label>
          <input type="text" id="city" name="city" value="<%= note.location.city %>" required>
        </div>
        <div class="form-group">
          <label for="country">Country:</label>
          <input type="text" id="country" name="country" value="<%= note.location.country %>" required>
        </div>
        <div class="form-group">
          <label for="latitude">Latitude:</label>
          <input type="number" step="any" id="latitude" name="latitude" value="<%= note.location.latitude %>" required>
        </div>
        <div class="form-group">
          <label for="longitude">Longitude:</label>
          <input type="number" step="any" id="longitude" name="longitude" value="<%= note.location.longitude %>" required>
        </div>
        <div class="form-group textarea-full">
          <label for="experience">Experience:</label>
          <textarea id="experience" name="experience" required><%= note.experience %></textarea>
        </div>
        <div class="form-group">
          <label for="weather_temperature">Temperature (°C):</label>
          <input type="number" id="weather_temperature" name="weather[temperature]" value="<%= note.weather.temperature %>" required>
        </div>
        <div class="form-group">
          <label for="weather_condition">Weather Condition:</label>
          <input type="text" id="weather_condition" name="weather[condition]" value="<%= note.weather.condition %>" required>
        </div>
        <button type="submit" class="btn btn-primary">Update Note</button>
      </form>
    </section>
  </main>


</body>
</html>